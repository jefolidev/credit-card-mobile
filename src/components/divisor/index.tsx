import { View } from 'react-native'

export function Divisor() {
  return (
    <View
      style={{
        backgroundColor: 'white',
        width: '100%',
        height: 0.5,
        borderRadius: 2,
        marginBlock: 2,
      }}
    />
  )
}
